{
  "routes": [
    { "src": "/data/.*\\.json", "dest": "/data/$1" },
    { "src": "/[^.]+", "dest": "/", "status": 200 }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app"
}